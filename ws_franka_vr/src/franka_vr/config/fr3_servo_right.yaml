# 右臂 MoveIt Servo 配置(前缀 right_ + 命名空间 /right)。
publish_period: 0.005
max_expected_latency: 0.03   # 降传输延迟, 拓宽反馈稳定裕度

command_in_type: "speed_units"
scale:
  linear:  0.2
  rotational:  0.8
  joint: 0.5

command_out_type: trajectory_msgs/JointTrajectory

publish_joint_positions: true
publish_joint_velocities: true
publish_joint_accelerations: false

use_smoothing: false
smoothing_filter_plugin_name: "online_signal_smoothing::AccelerationLimitedPlugin"

is_primary_planning_scene_monitor: true
check_octomap_collisions: false

## 组名(前缀化)
move_group_name: right_fr3_arm

lower_singularity_threshold: 17.0
hard_stop_singularity_threshold: 30.0
leaving_singularity_threshold_multiplier: 2.0
joint_limit_margins: [0.15]
halt_all_joints_in_cartesian_mode: false   # 单关节顶限位时"只停该关节", 其余关节继续用冗余跟随(默认true=整臂停, 浪费7DOF冗余)

## 话题(命名空间 /right)
cartesian_command_in_topic: ~/delta_twist_cmds
joint_command_in_topic: ~/delta_joint_cmds
joint_topic: /right/franka/joint_states
status_topic: ~/status
command_out_topic: /right/right_fr3_arm_controller/joint_trajectory

check_collisions: true
collision_check_rate: 10.0
self_collision_proximity_threshold: 0.01
scene_collision_proximity_threshold: 0.02
