# ROS2 / colcon 编译产物（换机器后 colcon build 会重新生成，不要提交）
build/
install/
log/

# Python 缓存
__pycache__/
*.pyc
*.pyo
*.egg-info/

# 诊断录制产物（diag_record.py 每次跑都会新生成，几十 MB，不入库）
diag_logs/

# 编辑器 / 系统
.vscode/
.idea/
*.swp
.DS_Store

# 编译日志（build_all.sh 输出，不入库）
build.log
*.log
